<!DOCTYPE html>
<html lang="lt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{url}}style.css">
    <title>{{title}}</title>
</head>

<body>
    {{#if hideMenu}}
    {{else}}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand-lg bg-body-tertiary">
                    <div class="container-fluid">
                        <a class="navbar-brand" href="{{url}}">Fūr Istai</a>
                        <div class="collapse navbar-collapse">
                            <div class="navbar-nav">
                                <a class="nav-link" href="{{url}}">Drivers List</a>
                                <a class="nav-link" href="{{url}}create">Add new Driver</a>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    {{/if}}